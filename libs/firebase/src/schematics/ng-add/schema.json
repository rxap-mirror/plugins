{
  "$schema": "http://json-schema.org/draft-07/schema",
  "$id": "firebase-ng-add",
  "type": "object",
  "properties": {
    "init": {
      "type": "boolean",
      "description": "Whether to initialize the firebase related projects and configurations",
      "default": true,
      "x-prompt": "Should the firebase related projects and configurations be initialized"
    },
    "functions": {
      "type": "boolean",
      "description": "Whether the functions project should be created",
      "default": false,
      "x-prompt": "Should the firebase functions project be created and configured?"
    }
  },
  "required": []
}
