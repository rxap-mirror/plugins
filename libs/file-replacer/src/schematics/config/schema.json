{
  "$schema": "http://json-schema.org/schema",
  "$id": "file-replacer-config",
  "title": "Config",
  "type": "object",
  "properties": {
    "project": {
      "type": "string",
      "description": "The name of the project.",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "For which project should the builder be added?"
    }
  },
  "required": [
    "project"
  ]
}
